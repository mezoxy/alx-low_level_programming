#include <stdio.h>
#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a = '_', b = 'p', c = 'u', d = 't', e = 'c';
	char f = 'h', g = 'a', h = 'r', i = '\n';

	_putchar(a);
	_putchar(b);
	_putchar(c);
	_putchar(d);
	_putchar(e);
	_putchar(f);
	_putchar(g);
	_putchar(h);
	_putchar(i);
	return (0);
}
