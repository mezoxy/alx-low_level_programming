#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int x = 48;

	for ( ; x <= 57 ; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
